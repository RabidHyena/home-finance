# OpenRouter API Key (required for AI parsing)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# AI Model (optional, default: google/gemini-3-flash-preview)
# OPENROUTER_MODEL=google/gemini-3-flash-preview

# Database (optional, defaults work with docker-compose)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/home_finance

# JWT Secret Key (REQUIRED in production — use a random string of 32+ characters)
SECRET_KEY=your-secret-key-here

# Debug mode (set to false in production)
DEBUG=true

# Seed admin password for initial migration (change after first login)
# SEED_ADMIN_PASSWORD=changeme
