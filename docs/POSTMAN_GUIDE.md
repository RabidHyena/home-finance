# Postman Testing Guide

## 🚀 Быстрый старт

### 1. Импортируйте collection
```
Postman → File → Import → выберите:
docs/Home_Finance_API.postman_collection.json
```

### 2. Откройте консоль Postman
```
View → Show Postman Console
```

### 3. Выполните Login (ОБЯЗАТЕЛЬНО!)
```
Auth → Login → Send
```

В консоли должно появиться:
```
✅ Access token saved to collection variables
```

### 4. Тестируйте API
Теперь можете выполнять любые запросы:
- Create Transaction
- Get Transactions
- Create Budget
- И т.д.

## 🔐 Как работает аутентификация

Collection автоматически управляет JWT токенами:
1. **Login** сохраняет токен в переменную `{{accessToken}}`
2. **Pre-request Script** в каждом защищённом запросе автоматически добавляет токен в Cookie header
3. **Logout** очищает токен

## ⚠️ Если получаете 401 Unauthorized

1. Откройте консоль Postman
2. Выполните Login
3. Проверьте что в консоли: `✅ Access token saved`
4. Попробуйте запрос снова

Если в консоли пишет `❌ NO TOKEN!` - значит Login не выполнен.

## 📊 Collection Runner

Для автоматического прогона всех тестов:
1. ПКМ на Collection → Run collection
2. Выберите запросы
3. Run Home Finance API

## 🎯 Рекомендуемый порядок

1. Health Check (опционально)
2. **Login** (обязательно!)
3. Create Transaction
4. Get All Transactions
5. Update Transaction
6. Create Budget
7. Get Budgets Status
8. Analytics endpoints
9. Logout

## 📝 Переменные Collection

Автоматически заполняются:
- `{{accessToken}}` - JWT токен (после Login)
- `{{transactionId}}` - ID созданной транзакции
- `{{budgetId}}` - ID созданного бюджета

## ✨ Особенности

- **Register** использует поле `username` (НЕ `login`)
- **Login** использует поле `login`
- Backend не поддерживает поле `type` (income/expense)
- Все денежные значения - числа (не строки)
- Даты должны быть в диапазоне 2000-2100

## 🆘 Troubleshooting

### Internal Server Error
Проверьте логи: `docker-compose logs backend`

### Validation Error (422)
Проверьте формат данных в Body запроса

### Unhealthy containers
```bash
docker-compose ps
docker-compose restart
```

---

Удачного тестирования! 🎉
